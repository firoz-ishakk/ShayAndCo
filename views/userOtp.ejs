<section class="vh-100 gradient-custom">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card bg-dark text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">

            <div class="mb-md-5 mt-md-4 pb-5">
              <form action="/otpverification" method="post">
              <h2 class="fw-bold mb-2 text-uppercase">OTP SIGN IN</h2>
              <p class="text-white-50 mb-5">ENTER YOUR OTP</p>


              <div class="form-outline form-white mb-4">
                <input type="password" name="otp" id="typePasswordX" class="form-control form-control-lg" />
                <label class="form-label" for="typePasswordX">OTP</label>
              </div>
             
              <p class="small mb-5 pb-lg-2"><a class="text-white-50" href="/Otpfail">CANCEL VERIFICTATION?</a></p>

              <button class="btn btn-outline-light btn-lg px-5" type="submit">Verify OTP</button>

              <div class="d-flex justify-content-center text-center mt-4 pt-1">
              </div>
              </form>
              <form class="row contact_form" action="/resendotp" method="get" >
                              
                <button type="submit" id="" value="submit" onclick="countdown()" class="btn_3">
                    Resend otp
                </button>
            </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
       
  var timeLeft = 100;
var elem = document.getElementById('some_div');
var timerId = setInterval(countdown, 1000);

function countdown() {
document.getElementById("my_button").style.visibility = "hidden";
if (timeLeft == -1) {
   clearTimeout(timerId);
   doSomething();
} else {
   elem.innerHTML = timeLeft + ' seconds remaining';
   timeLeft--;
}
}

function doSomething() {

document.getElementById("my_button").style.visibility = "visible";
}


</script>

